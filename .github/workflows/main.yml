name: Mi primer workflow

on:
  push:
    branches: [ "main" ]

jobs:
  saludo:
    runs-on: ubuntu-latest
    steps:
      - name: Mostrar mensaje
        run: echo "Hola Andrés, estás practicando GitHub Actions 🚀"
